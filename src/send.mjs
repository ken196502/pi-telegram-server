#!/usr/bin/env node
import path from "node:path";
import { loadEnvFile } from "./lib.mjs";
loadEnvFile(path.resolve(".env"));
function usage() { return "Usage: npm run send -- --to <chat-id-or-@username> [--message <text>] [--file <local-path>]\n\nEnvironment:\n  PI_TELEGRAM_WEBHOOK_URL or TELEGRAM_WEBHOOK_URL  webhook URL\n  PI_TELEGRAM_WEBHOOK_TOKEN or WEBHOOK_TOKEN        webhook token\n  TELEGRAM_TO                                      default recipient"; }
function parseArgs(args) { const options = {}; for (let i = 0; i < args.length; i += 1) { const arg = args[i]; if (arg === "--help" || arg === "-h") return { help: true }; if (!["--to", "--message", "--file", "--file-name", "--mime-type", "--url"].includes(arg)) throw new Error(`unknown option: ${arg}`); const value = args[++i]; if (!value || value.startsWith("--")) throw new Error(`${arg} requires a value`); options[arg.slice(2)] = value; } return options; }
async function main() { const options = parseArgs(process.argv.slice(2)); if (options.help) return console.log(usage()); const host = process.env.TELEGRAM_HOST || "127.0.0.1"; const port = process.env.TELEGRAM_PORT || "3093"; const url = options.url || process.env.PI_TELEGRAM_WEBHOOK_URL || process.env.TELEGRAM_WEBHOOK_URL || `http://${host}:${port}/webhook`; const token = process.env.PI_TELEGRAM_WEBHOOK_TOKEN || process.env.TELEGRAM_WEBHOOK_TOKEN || process.env.WEBHOOK_TOKEN || ""; const to = options.to || process.env.TELEGRAM_TO || ""; if (!token) throw new Error("PI_TELEGRAM_WEBHOOK_TOKEN or WEBHOOK_TOKEN is required"); if (!to) throw new Error(`--to or TELEGRAM_TO is required\n\n${usage()}`); if (!options.message?.trim() && !options.file) throw new Error(`--message or --file is required\n\n${usage()}`); const payload = { to, message: options.message || "" }; if (options.file) payload.filePath = options.file; if (options["file-name"]) payload.fileName = options["file-name"]; if (options["mime-type"]) payload.mimetype = options["mime-type"]; const response = await fetch(url, { method: "POST", headers: { authorization: `Bearer ${token}`, "content-type": "application/json" }, body: JSON.stringify(payload) }); const raw = await response.text(); let body; try { body = JSON.parse(raw); } catch { body = { error: raw }; } if (!response.ok) throw new Error(`webhook failed with HTTP ${response.status}: ${body.error || raw}`); console.log(JSON.stringify(body)); }
main().catch((e) => { console.error(`telegram send: ${e.message}`); process.exitCode = 1; });
